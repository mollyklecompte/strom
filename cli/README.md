### Install:
  * ``` cd strom/CLI/ ```
    * ``` pip install (dot) ```
    * For auto-complete in Bash:
      * ``` eval "$(_DSTREAM_COMPLETE=source dstream)" ```
### Run:
  * ``` dstream --help ```

#####Download
#####Install
#####Run
#####Usage
#####Help