#####Download:
#####Install:
  * ``` cd strom/CLI/ ```
    * ``` pip install (dot) ```
    * For auto-complete in Bash:
      * ``` eval "$(_DSTREAM_COMPLETE=source dstream)" ```
#####Run:
    * ``` dstream --help ```
#####Usage:
### Install:
#####Help:
    *   Hard Errors(Red):
        *   Server failure
    *   Soft Errors(Yellow):
        *   Syntax issue